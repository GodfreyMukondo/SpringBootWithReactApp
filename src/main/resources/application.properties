# App Name
spring.application.name=SpringBootWithReactApp

# MongoDB Atlas Connection URI
spring.data.mongodb.uri=mongodb+srv://godfrey:1234@cluster0.42uhykl.mongodb.net/JobListing?retryWrites=true&w=majority&tls=true

# Optional: Specify the database name (redundant if already in URI)
spring.data.mongodb.database=JobListing

# Disable SSL if you're testing locally, though MongoDB Atlas uses SSL
server.ssl.enabled=false
